require('dotenv').config(); const { getAccessToken } = require('./netsuite-auth.js'); getAccessToken().then(async token => { try { const r = await fetch('https://4815401.restlets.api.netsuite.com/app/site/hosting/restlet.nl?script=946&deploy=1&limit=10', { headers: { 'Authorization': 'Bearer ' + token, 'Accept': 'application/json' } }); console.log(r.status, await r.text()); } catch (e) { console.error(e); } });
